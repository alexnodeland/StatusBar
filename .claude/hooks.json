{
  "hooks": [
    {
      "matcher": "Bash(make build*)",
      "hooks": [
        {
          "type": "afterToolResult",
          "command": "make lint 2>&1 | tail -5"
        }
      ]
    }
  ]
}
